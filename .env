# .env

# PostgreSQL database (primary)
POSTGRES_URL=postgresql://postgres:password@localhost:5432/id_card_db

# SQLite fallback (auto-used if PostgreSQL fails)
SQLITE_URL=sqlite:///./local.db
VITE_API_BASE_URL=http://localhost:8000
